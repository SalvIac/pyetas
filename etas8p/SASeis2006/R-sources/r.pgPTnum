  par(mfrow=c(2,1))
  data<- scan('out.pgPTnum'); H <- data[1]; Tend <- data[2]
  data<- matrix(scan('out.pgPTnum', skip=2), ncol=2, byrow=T)
  plot(data[,1],data[,2],type="l",ylab="Number of Events in [tau, tau+h]", xlab="tau = Time x (Total Number of Events) / (Time End)",xlim=c(0,Tend))
  abline(h=seq(-3,3,1),lty=2, col=2)
 title(main=paste("PGRAPH: Normalized number of point in [t, t+h], where h=",H,sep=""))
  data<- scan('out.pgCumMT'); Tend <- data[1]; NN <- data[2]
  data<- matrix(scan("out.pgCumMT", skip=2), ncol=2, byrow=T)
  plot(data[,1],data[,2],type="h", ylab="Magnitude", xlab="Ordinary or Transformed Time",xlim=c(0,Tend))
#########################################################################
  postscript("pgPTnum.ps",horizontal=F)
  par(mfrow=c(2,1))
  data<- scan('out.pgPTnum'); H <- data[1]; Tend <- data[2]
  data<- matrix(scan('out.pgPTnum', skip=2), ncol=2, byrow=T)
  plot(data[,1],data[,2],type="l",ylab="Number of Events in [tau, tau+h]", xlab="tau = Time x (Total Number of Events) / (Time End)",xlim=c(0,Tend))
  abline(h=seq(-3,3,1),lty=2, col=2)
 title(main=paste("PGRAPH: Normalized number of point in [t, t+h], where h=",H,sep=""))
  data<- scan('out.pgCumMT'); Tend <- data[1]; NN <- data[2]
  data<- matrix(scan("out.pgCumMT", skip=2), ncol=2, byrow=T)
  plot(data[,1],data[,2],type="h", ylab="Magnitude", xlab="Ordinary or Transformed Time",xlim=c(0,Tend))
  dev.off()
